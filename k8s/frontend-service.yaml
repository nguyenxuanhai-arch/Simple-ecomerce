apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend-service
  labels:
    app: ecommerce-frontend
spec:
  type: NodePort
  selector:
    app: ecommerce-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
