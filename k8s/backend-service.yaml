apiVersion: v1
kind: Service
metadata:
  name: ecommerce-backend-service
  labels:
    app: ecommerce-backend
spec:
  type: NodePort
  selector:
    app: ecommerce-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
