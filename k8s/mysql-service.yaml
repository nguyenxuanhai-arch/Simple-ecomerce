apiVersion: v1
kind: Service
metadata:
  name: ecommerce-mysql
  labels:
    app: ecommerce-mysql
spec:
  type: ClusterIP
  selector:
    app: ecommerce-mysql
  ports:
    - name: mysql
      protocol: TCP
      port: 3306
      targetPort: 3306
